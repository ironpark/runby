# 사용자 가이드

[English](../../en/guide/) | 한국어 | [日本語](../../ja/guide/)

`runby`를 처음 사용한다면 [시작하기](getting-started.md)에서 설치와 핵심 API를 먼저 확인하세요. 바로 적용할 코드가 필요하다면 [활용 예제](recipes.md)에서 목적에 맞는 패턴을 골라 사용할 수 있습니다.

## 목적별로 찾기

| 하고 싶은 일 | 먼저 볼 문서 |
|---|---|
| Go 프로그램에 처음 연결하기 | [시작하기](getting-started.md) |
| 프롬프트·색상·진행 표시를 안전하게 제어하기 | [활용 예제](recipes.md#대화형-기능을-안전하게-결정하기) |
| 셸 스크립트에서 조건 분기하기 | [CLI 가이드](cli.md#셸에서-쓰기) |
| 결과를 로그나 JSON으로 남기기 | [활용 예제](recipes.md#실행-맥락을-로그로-남기기) |
| 테스트에서 환경을 재현하기 | [시작하기](getting-started.md#다른-환경을-넘기기) |
| 지원하지 않는 환경 추가하기 | [드라이버 만들기](drivers.md) |
| 필드와 옵션 전체를 찾기 | [API 레퍼런스](api.md) |
| 감지 규칙의 근거를 확인하기 | [조사 문서](../../research/) |

## 처음 읽는 순서

1. [시작하기](getting-started.md)에서 `Current()`와 `Detect()` 중 무엇을 쓸지 결정합니다.
2. [활용 예제](recipes.md)에서 프로그램의 동작을 결정하는 패턴을 고릅니다.
3. 필요한 축의 상세 문서만 읽습니다. 모든 축을 이해해야 사용할 수 있는 라이브러리는 아닙니다.
4. 고급 옵션이나 확장이 필요할 때 [API](api.md)와 [드라이버](drivers.md)를 확인합니다.

## 개념별 상세

| 문서 | 답하는 질문 | 주요 결과 |
|---|---|---|
| [에이전트](agents.md) | AI 에이전트가 실행했으며 계층은 무엇인가? | `Agents`, `Chain()` |
| [CI](ci.md) | 어느 CI 플랫폼과 잡에서 실행 중인가? | `CI` |
| [실행 도구](runner.md) | npm·make·systemd 같은 무엇이 실행했나? | `Runners` |
| [원격 환경](remote.md) | SSH·tmux·개발 컨테이너를 거쳤나? | `Remotes` |
| [터미널과 TTY](terminal.md) | 환경을 만든 앱은 무엇이고, 지금 대화형인가? | `Terminal`, `TTY` |
| [상위 프로세스](process.md) | 감지된 실행 주체가 현재도 조상으로 살아 있나? | `Process`, `AncestorPID` |

여러 축은 동시에 참일 수 있습니다. 예를 들어 Claude Code가 GitHub Actions 안에서 `npm test`를 실행하면 `Agents`, `CI`, `Runners`가 모두 채워집니다. 하나의 “실행 모드”를 찾기보다, 필요한 질문에 해당하는 필드를 선택하세요.

## 해석할 때 지킬 원칙

- 환경변수 기반 감지는 프로세스가 시작될 때 상속된 상태입니다. 현재 생존 상태가 필요하면 [상위 프로세스 교차 검증](process.md#교차-검증)을 사용합니다.
- `Terminal`은 에뮬레이터의 정체성이고 `TTY`는 현재 스트림 연결 상태입니다. 프롬프트 여부에는 `TTY.Interactive`를 사용합니다.
- `AncestorPID != 0`은 강한 확증입니다. 반대로 `0`은 읽기 제한이나 실행 구조 때문일 수 있으므로 부정의 근거가 아닙니다.
- `-json` 결과에는 세션 ID와 로컬 경로가 포함될 수 있습니다. 외부 공유용 진단에는 `runby -v`가 더 안전합니다.

감지 결과의 공식 근거, 검증 날짜, 감지에서 제외한 제품은 [조사 문서](../../research/)에서 확인할 수 있습니다.
